namespace ShapeUp.Features.Editor2D;

/// <summary>Active 2D viewport tool (toolbar + keyboard).</summary>
public enum Editor2DTool
{
    Select,
    Move,
    Rotate,
    Draw,
    Cut,
    Measure,
}
